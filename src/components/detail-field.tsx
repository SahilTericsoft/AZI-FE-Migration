/**
 * Back-compat shim. The detail field/section now live in `@/components/ui/detail`
 * (shadcn). Re-exported here so existing imports keep working.
 */
export { DetailField, DetailSection } from "@/components/ui/detail";
